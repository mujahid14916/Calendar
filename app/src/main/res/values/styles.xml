<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Table">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/tableBG</item>
        <item name="android:onClick">displace</item>
        <item name="android:layout_marginBottom">@dimen/monthMargin</item>
    </style>

    <style name="Month">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">@dimen/monthSize</item>
        <item name="android:textColor">@color/monthColor</item>
        <!--<item name="android:textStyle">bold</item>-->
    </style>

    <style name="WeekNames">
        <item name="android:layout_width">@dimen/dayWidth</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/otherDays</item>
        <item name="android:textAlignment">center</item>
        <!--<item name="android:background">@drawable/stroke</item>-->
        <item name="android:textStyle">bold</item>
    </style>

</resources>
